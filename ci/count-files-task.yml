platform: linux

image_resource:
  type: docker-image
  source:
    repository: ubuntu

inputs:
  - name: my-repo

run:
  path: sh
  args:
    - -exc
    - |
      echo "Counting files in repository..."
      count=$(find my-repo -type f | wc -l)
      echo "Number of files in repo: $count"
